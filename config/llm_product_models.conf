# Modeles LLM par produit (source operationnelle — pousse en base via set_product_llm_models.ps1)
# Format: product_code <tab|espace> chapter_model <tab|espace> summary_model [provider]
# Lignes vides et # commentaires ignores.

natal_prompter	gpt-5.4-mini	gpt-5-nano	openai
