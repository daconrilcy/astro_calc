{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "integration_job_request_v1",
  "description": "Enveloppe POST /v1/jobs. Le champ payload est opaque ici ; valide ensuite contre payload_contract du service_code.",
  "type": "object",
  "required": ["service_code", "payload"],
  "additionalProperties": false,
  "properties": {
    "service_code": {
      "type": "string",
      "minLength": 1
    },
    "payload": {
      "type": "object",
      "description": "Objet metier opaque valide contre le payload_contract du service catalogue."
    },
    "user_language": {
      "type": "string",
      "default": "fr"
    },
    "audience_level": {
      "type": "string",
      "enum": ["beginner", "intermediate", "expert"],
      "default": "beginner"
    },
    "astrologer_profile": {
      "type": "object",
      "description": "Style redactionnel ; structure detaillee selon service."
    }
  }
}
