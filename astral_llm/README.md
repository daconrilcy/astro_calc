# astral_llm

Crate reserve pour l'integration LLM (prompting, appels modele, orchestration
cote modele). Le moteur de calcul et la projection JSON restent dans
`astral_calculator`.

```powershell
cargo build -p astral_llm
```
